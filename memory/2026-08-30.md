# 2026-08-30

## 今日结论

完成旧 VuePress 博客向数据库博客的迁移、网页发布后端、COS 图片链路和 SheepIM 聊天能力收敛。后端已用不可变 CCR 镜像发布到生产 Kubernetes，旧 SheepIM、Redis、Kafka 和 etcd 业务工作负载已停用；生产节点内存恢复到健康余量。

## 博客与聊天整合

- 新增博客文章、修订、资源和迁移标记模型；公开接口按 slug/旧 permalink 读取已发布文章，管理接口支持创建、草稿保存、发布、撤回、归档、删除、修订列表和历史恢复。
- 草稿保存使用版本号做并发冲突检测，每次保存保留修订记录；网页编辑不依赖 Git，也不触发前端重新部署。
- 图片使用腾讯云 COS 预签名 PUT，后端签名、HEAD 确认并提供稳定资源地址；凭据不下发到浏览器。
- 将旧 VuePress 内容导出为内嵌迁移数据，启动时通过迁移标记幂等导入；生产数据库共导入并公开 96 篇文章，旧 `/pages/<slug>/` permalink 可继续定位同一文章。
- 将房间创建、加入和 WebSocket 消息中继收敛进 `personal-page-be`；当前实现为单副本进程内房间，限制两人、带 TTL，匹配现有前端协议。
- 新增共享 Web Origin 校验，允许正式站、Pages 项目域、单级 Pages 预览子域和本地开发来源，并拒绝 HTTP、相似域及带路径/查询的伪造来源；HTTP API 和 WebSocket 共用该规则。

## 验证

- `go test ./...`、`go vet ./...`、Go 构建、Linux/amd64 构建、`gofmt` 与 `git diff --check` 通过。
- 生产 `/api/ping` 返回 200；`/api/blog/posts?pageSize=1` 返回 `total: 96`；旧 permalink 和新 slug 均在真实浏览器中返回正确正文。
- 两客户端房间创建、加入、鉴权和消息互通 smoke 已通过。
- Pages 正式域和预览子域的 OPTIONS 均返回 204 与精确 `Access-Control-Allow-Origin`；仿冒域返回 403。

## 提交与生产发布

- 博客与聊天整合提交：`48af7a5`；迁移字段修复：`e946571`；Pages Origin 修复：`5a31a61`，均已推送到 `origin/master`。
- 最终生产镜像：`ccr.ccs.tencentyun.com/littlehorse/personal-page-be:5a31a6124fdcfac7760c3dee689af0aba86c417e@sha256:5e1ffa95b3aa32ba31ecca687d03cbc4aca37f332ae51958803d1bda6aef40ce`。
- 生产 Pod：`personal-page-be-deployment-6d6fbfc4fd-sdqd4`，Ready，重启 0；Deployment 为 1 副本，请求 20m CPU/64MiB、限制 500m CPU/256MiB。
- 发布前数据库备份：`/home/ubuntu/backups/personal-page/pre-blog-20260830-175537.sql`。
- 部署仓库提交 `64d3078` 将旧 SheepIM 服务和 Redis 缩到 0，并发布单体后端；提交 `b98cc3f` 同步 Nginx Pages CORS 白名单。
- 最终生产节点内存为 7.3GiB 总量、1.9GiB 已用、5.1GiB available；未启用 swap，避免单节点 Kubernetes 在内存压力下隐藏问题。

## 已知事项

- 生产日志在 10:53 记录一次 Dify 上游错误 `Model deepseek-chat not exist`。博客发布、图片、文章读取和站内双人聊天不依赖该模型；AI 对话/续写需要在 Dify 应用中改为账号当前可用的模型后再单独验证。
- 当前聊天房间存在单个后端 Pod 内存中；若以后扩到多副本，需要引入共享房间状态和跨实例消息总线，不能直接增加副本数。


# 2026-08-31

## 博客多篇置顶

- 放弃给所有文章设置 1–5 重要程度，改为文章级的多篇置顶：`blog_posts` 新增 `pinned`、`pinned_at`，不写入 revision，置顶操作不会产生文章版本。
- 新增超级管理员接口 `POST /api/admin/blog/posts/:id/pin`，请求体为 `{ "pinned": true|false }`。每次置顶都会刷新 `pinned_at`，因此可通过取消后重新置顶调整多篇置顶文章的先后。
- 公开和后台列表默认按 `pinned DESC, pinned_at DESC, published_at/created_at DESC` 排序；`sort=time` 可完全忽略置顶并按发布时间倒序。
- 列表查询统一关联当前 revision，分类、标签、搜索与两种排序可以同时使用。

## 验证与发布

- `go test ./...`、`go vet ./...`、`git diff --check` 通过；新增 `normalizeBlogSort` 单元测试覆盖默认、时间、大小写和无效值。
- 上线前全库备份：`/home/ubuntu/backups/personal-page/pre-pinning-20260831-222633.sql`，346745 bytes，SHA-256 `f6be9356d3803d99e9375f10f0f19a95f43cde6eabe9f22b197ce2509e6f4bca`。
- 功能提交：`7dd38dbdc03befa80247a57ece2376d30b3d24ae`。
- 生产镜像：`ccr.ccs.tencentyun.com/littlehorse/personal-page-be:7dd38dbdc03befa80247a57ece2376d30b3d24ae@sha256:92a0e7f437e2c06098bdfb96a968e7c2aeef8d3fdf66772c9fd9405552323ec1`。
- Kubernetes rollout 成功，新 Pod 最近日志无 panic/fatal/error；生产 API 返回 96 篇文章、`pinned` 字段存在、`category=web` 返回 8 篇、`sort=time` 正常。

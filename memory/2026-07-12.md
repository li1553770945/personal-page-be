# 2026-07-12

## 今日结论

完成 AI 对话安全加固、生产崩溃修复和后端发布。代码已推送，生产 Kubernetes revision 27 运行两个新副本，均为 Ready 且发布后重启次数为 0。

## 代码变更

- 应用提交：`08f3179df68c7a62c720b109d2b3d9a960497a7d`，提交信息为 `feat: harden AI chat request handling`。
- 强制校验 JWT 密钥，Session 和 AI 访客身份密钥通过用途隔离派生，移除不安全的硬编码默认值。
- 新增 AI 请求体、消息长度、分钟请求、并发请求和每日预算限制。
- 匿名访客和登录用户使用 HMAC 伪匿名身份；用量表只保存摘要，不保存原始访客 ID 或 IP。
- 每日预算通过 MySQL 事务和稳定锁顺序原子预占，多副本不会突破每日上限。
- Dify SSE 使用请求上下文，在客户端断开或写入失败时取消上游请求。
- 收紧 Bearer JWT 算法校验，统一 AI 错误使用 400、413、429、503 等标准 HTTP 状态。
- 修复 `biz/internal/service/global_service/cron.go`：`/app/files` 不存在或不可读时不再 panic，启动和清理前会安全创建目录并记录错误。
- GitHub Actions 增加完整 Git SHA 镜像标签，但本日 Docker Hub 登录凭据失效，远端镜像推送未完成。

## 验证

- `go test ./...` 通过。
- 核心包重复测试通过。
- `go vet ./...` 通过。
- Linux amd64 静态编译通过。
- 本次变更涉及的 Go 文件均通过 `gofmt` 检查，`git diff --check` 通过。
- 本机没有 GCC/CGO，未执行 race 测试。
- 线上 AI smoke：空消息返回 400，超过 64 KiB 的请求体返回 413，真实 Dify SSE 请求成功完成。
- 主动断开 SSE 后，Pod 日志出现 `AI chat stream canceled`，确认上游取消链路生效。

## 生产发布

- 生产主机：`ubuntu@124.223.181.152`，节点名 `master-node`。
- Kubernetes：单节点 v1.31.2，后端 Deployment 为 `personal-page-be-deployment`。
- 生产镜像：`peacesheep/personal-page-be:08f3179df68c7a62c720b109d2b3d9a960497a7d`。
- 镜像 ID：`sha256:1cf530828cd64cc4de12d8a77f11470168cd452921e46868c092d0ab92322c92`。
- containerd manifest digest：`sha256:cc39f605266eb207b9eeec8db23a497977263a49696a5466c065f412535eedbb`。
- 服务器无法稳定连接 Docker Hub，因此使用已验证的 Linux/amd64 二进制封装精确镜像，并离线导入 containerd。
- JWT Secret 与原 ConfigMap JWT 密钥长度和哈希一致，没有发生 JWT 或受保护幻灯片加密密钥切换。
- NodePort `http://127.0.0.1:31902/api/ping` 与公网 `https://api.peacesheep.xyz/api/ping` 均返回健康结果。
- K8s 回滚文件：`/tmp/personal-page-be-deployment-before-08f3179.yaml`。

## 已知风险与待办

- GitHub Actions `29194195041` 在 Docker Hub 登录步骤失败；需要更新仓库 Secret `DOCKER_TOKEN`，否则节点丢失后无法从仓库重拉本版本。
- 分钟和并发限制仍是进程内实现；两个副本下理论上约为 12 次/分钟、2 个并发。MySQL 每日预算仍是全局原子限制。
- IP 维度会让同一 NAT 后的用户共享部分额度，这是当前产品取舍。
- Session 签名从硬编码值切到安全派生值，已有登录 Cookie 会要求用户重新登录一次。
- 全局请求体默认上限从 512 MiB 降为 32 MiB；旧 multipart 或大 ZIP 请求超过该值会收到 413。
- `/app/files` 仍是 Pod 本地临时存储，双副本不共享且重建会丢失。后续应迁移到 COS 或持久化共享卷。

## 记忆文件调整

- 新增本仓库专属中文 `AGENT.md` 和 `memory/2026-07-12.md`。
- 本文件只维护后端事实；前端发布记录由 `personal-page-fe-new/memory/` 独立维护。

## GitHub Actions Docker 发布修复

- 复核失败运行 `29194195041`：`docker/login-action` 在发起 Docker Hub 认证前即报 `Error: Password required`，说明 `${{ secrets.DOCKER_TOKEN }}` 在该运行中解析为空；这是 Secret 缺失、为空、名称不一致或作用域不可用，不是 Node 版本导致的登录失败。
- 更新 `.github/workflows/build.yml`：`actions/checkout@v4` 升级到 Node 24 运行时的 `@v7`，`docker/login-action@v3` 升级到 Node 24 运行时的 `@v4`。
- 登录前新增 `DOCKER_TOKEN` 空值校验，后续缺少 Secret 时会输出明确的仓库配置错误，不再只显示 `Password required`。
- 新增 `workflow_dispatch` 手动触发入口以及最小 `contents: read` 权限。
- 手动运行的发布 Job 仅允许 `master` 分支，避免误选其他 ref 后覆盖 Docker Hub 的 `latest` 标签。
- `go run github.com/rhysd/actionlint/cmd/actionlint@latest .github/workflows/build.yml` 校验通过。
- 外部待办：仓库管理员仍需在 GitHub Actions repository secrets 中创建或修复名称精确为 `DOCKER_TOKEN` 的 Docker Hub access token；不得把 Token 原文写入代码或本记忆文件。
